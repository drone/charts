apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drone-runner-docker.fullname" . }}-test-connection"
  labels:
    {{- include "drone-runner-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drone-runner-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
